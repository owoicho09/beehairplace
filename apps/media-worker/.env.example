PORT=8787

# Shared secret with the web app (must match MEDIA_WORKER_SECRET in apps/web/.env)
MEDIA_WORKER_SECRET=

# Supabase project (same project as the web app)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PUBLIC_MEDIA_BUCKET=media
